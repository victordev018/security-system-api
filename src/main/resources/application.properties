spring.jpa.open-in-view=false

# environment variables with default values
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/secutity_monitor}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:jotave9474}

# Hibernate configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Raspberry pi pico W server configuration
raspberry.server.ip=${RASP_IP:10.0.0.118}
raspberry.server.port=${RASP_PORT:8050}